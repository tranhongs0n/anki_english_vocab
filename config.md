# Anki Vocab Suite Config

- **shortcuts**: Global and reviewer shortcuts (F4-F10, Ctrl+Y, zoom).
- **decks**: Target and triage decks.
- **note_types**: Note type and field mappings.
- **llm**: DeepSeek API URL, Key, Model fallback list.
- **features**: Toggles for background worker, zoom persist, pre-sync pipeline, git repo path.
